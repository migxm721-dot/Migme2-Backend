spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:migme_qa}
    username: ${DB_USERNAME:migmeuser}
    password: ${DB_PASSWORD:changeme}
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

logging:
  level:
    net.migxchat: INFO
    root: INFO
